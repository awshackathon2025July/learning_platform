version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip3 install --upgrade pip
      - pip3 install --no-cache-dir -r requirements.txt
      - echo "=== Moving to rival-backend directory ==="
      - cd rival-backend && ls -la
      - echo "=== Testing import ==="
      - cd rival-backend && python3 -c "try: from src.main import app; print('Import successful'); print('App object:', app); except Exception as e: print('Import failed:', e); import traceback; traceback.print_exc(); exit(1)"
run:
  command: sh -c "cd rival-backend && python3 -m gunicorn --bind 0.0.0.0:8080 --workers 1 src.main:app"
  network:
    port: 8080
